apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: communication-busybox-3-destinationrule
spec:
  host: communication-busybox-3
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
